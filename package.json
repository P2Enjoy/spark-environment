{
  "name": "spark-environment",
  "private": true,
  "version": "0.0.0",
  "description": "Plan de controle decoupant un serveur en cellules d'execution contingentees",
  "packageManager": "pnpm@9.15.4",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "pnpm --filter @spark/webui dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
