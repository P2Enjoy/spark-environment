{
  "name": "@spark/webui",
  "private": true,
  "version": "0.0.0",
  "description": "Console d'administration locale : SPA React et hote Node porteur des tunnels SSH",
  "type": "module",
  "scripts": {
    "dev": "echo 'Console non encore implementee — unites SPK-16 et SPK-18' && exit 1",
    "build": "echo 'rien a construire pour le moment' && exit 0",
    "test": "echo 'aucun test — la console n a pas encore de source' && exit 0",
    "typecheck": "echo 'aucune source a verifier' && exit 0"
  }
}
