{
  "name": "@spark/webui",
  "private": true,
  "version": "0.0.0",
  "description": "Console d'administration locale : hôte Node porteur des tunnels SSH, et SPA React",
  "type": "module",
  "scripts": {
    "dev": "node host/main.js",
    "build": "echo 'SPA non encore implémentée — unité SPK-18' && exit 0",
    "test": "node --test host/*.test.js",
    "typecheck": "tsc --noEmit --allowJs --checkJs false host/*.js"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
